# Disable directory listing
Options -Indexes

# Set default index files (priority order)
DirectoryIndex index.php index.html

# Block access to sensitive file types
<FilesMatch "\.(log|sh|sql|save|swp|conf|config|key|pem)$">
    Require all denied
</FilesMatch>

# Block all hidden files and directories
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteRule "/\." - [F]
</IfModule>

# Default allow rule for all other requests
Require all granted
